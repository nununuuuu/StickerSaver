## v0.2.5

### 新增功能
- 更新提示卡片會直接顯示本版本新增功能與修正項目。
- 更新卡片加入紅底「重要提醒」區塊。
- 正式版改用固定 Android release 簽章，之後可由 App 內直接原地更新，不再因每次 GitHub Actions 使用不同 debug 簽章而發生套件衝突。

### 修正項目
- 修正 App 內更新時出現「與現有套件衝突」的根本原因。
- GitHub Actions 改為使用固定 keystore、固定 alias 與固定密碼組合簽署 release APK。
- 正式建置由 assembleDebug 改為 assembleRelease，並在發布前以 apksigner 驗證簽章。
- 更新資訊解析支援「新增功能 / 修正項目」標題，避免更新卡片只有版本號而沒有內容。

### 重要提醒
- v0.2.5 是切換到固定正式簽章的第一版。由於手機目前安裝的舊版使用不同 debug 簽章，第一次必須先解除安裝舊版，再安裝 v0.2.5。
- 從 v0.2.5 開始，只要保留同一把 signing key，後續 v0.2.6、v0.2.7…即可直接在 App 內更新。
