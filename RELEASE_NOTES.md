# v0.3.6

## 新增功能
- 新增頁改成「解析預覽 → 確認匯入」兩階段流程，解析結果先顯示在頁面下方，不再直接寫入貼圖庫。
- 解析結果支援逐張選取、全選與全部取消，確認後才套用分類並加入貼圖庫。

## 修正項目
- 全 App Material 主要色系統一為暖白、米色與深灰，避免元件回落到預設紫色。
- 貼圖庫「最新 / 常用 / 全部」選取底板會跟隨左右滑動進度連續移動，降低分頁割裂感。
- 單張貼圖編輯視窗移除標題並放大貼圖預覽，統一輸入框、分類標籤與按鈕風格。
- 分類標籤管理改為緊湊可捲動列表，不再使用突兀的大白卡片，並以「分類名稱 · N 張」顯示使用數量。
- 改善 Sticker Saver 鍵盤返回上一個輸入法時的切換可靠性，若系統仍停留在本鍵盤會自動補一次切換。
